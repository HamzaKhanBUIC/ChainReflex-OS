# Local vLLM instance URI
VLLM_API_BASE=http://localhost:8000/v1

# Mock key (OpenAI SDK requires one, but local vLLM ignores it)
MOCK_API_KEY=dummy-rocm-key

# The exact model name you are serving via vLLM
LLM_MODEL_NAME=meta-llama/Meta-Llama-3-8B-Instruct
